# Laundry Management System

## Frontend

Main login page:

- `frontend/loginPage/index.html`

Home page after login:

- `frontend/loginPage/HomeSection/index.html`

## Run locally

Open PowerShell in `frontend` and run:

```powershell
python -m http.server 5500
```

Then open:

- `http://localhost:5500`
